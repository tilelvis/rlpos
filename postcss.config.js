// Explicitly empty PostCSS config. This file's presence at the project
// root is the strongest signal to Vite that there are no PostCSS plugins
// to load — Vite will not walk up the directory tree looking for one
// in a parent folder.
//
// If you ever want Tailwind, autoprefixer, etc., add them here.
export default {
  plugins: [],
};

{
  "name": "raicilabs-pos-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Raicilabs POS — HTML + Vite + PWA, with WebUSB thermal printer (WinUSB/Zadig ready).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "serve": "vite preview --host --port 4173",
    "build:tauri": "node scripts/vite-for-tauri.mjs build",
    "dev:tauri": "node scripts/vite-for-tauri.mjs dev",
    "tauri": "npx @tauri-apps/cli",
    "tauri:dev": "npx @tauri-apps/cli dev",
    "tauri:build": "npx @tauri-apps/cli build"
  },
  "dependencies": {
    "@fontsource/jetbrains-mono": "^5.2.6",
    "jspdf": "^2.5.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.5"
  }
}
